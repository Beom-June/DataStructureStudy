using System;
using System.Collections.Generic;
using System.Collections;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace DataStructureStudy
{
    
    // 큐 기본 구현
    /*
    class QueueBasic
    {
        static void Main(string[] args)
        {
            Queue _queue = new Queue();

            _queue.Enqueue(1);
            _queue.Enqueue(2);
            _queue.Enqueue(31);
            _queue.Enqueue("This is my string");

            _queue.Enqueue(null);
            _queue.Enqueue("This is the last value");

            // 위에 선언한 Enqueue를 다 세어서 카운트함
            Console.WriteLine($"Number of Elements = {_queue.Count}");

            Console.WriteLine($"The peak of _queue is = {_queue.Peek()}");
            Console.WriteLine($"Value 30 exists in _queue =>  {_queue.Contains(30)}");

            //_queue.Clear();               // Clear 시 0 나옴
            Console.WriteLine($"Number of Elements = {_queue.Count}");

        }
    }
    */
}
